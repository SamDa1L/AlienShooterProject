# 项目协作规则

本文件适用于当前仓库根目录及其所有子目录。后续所有自动化助手、代码代理和文档产出都必须遵守以下规则。

## 一、文档语言规则

- 所有文档型产出必须以中文内容为主，不能出现纯英文说明段落，也不能出现乱码内容。
- 允许保留必要的英文专有名词、文件路径、类名、方法名、字段名、命名空间、插件名和技术名词。
- 如果引用或分析外部资源时遇到编码异常，必须先修复或说明编码问题，不能把乱码直接写入正式文档。
- 文档中的代码块可以包含英文标识符，但代码块外的解释说明必须使用中文。

## 二、Git 操作规则

- 未经用户明确允许，不能私自执行任何 Git 上传、提交或历史修改操作。
- 禁止擅自执行 `git commit`、`git push`、`git pull`、`git merge`、`git rebase`、`git reset`、`git stash`、`git tag`、创建或切换分支等会改变仓库状态或远端状态的操作。
- 如确实需要 Git 操作，必须先说明原因、影响范围和具体命令，等待用户明确确认。
- 允许在需要时读取 Git 信息，例如查看状态、差异、日志或责任行，但不能擅自修改 Git 状态。

## 三、原创命名规则

- 即使参考其他项目、模板工程或第三方 Demo，也不能在正式项目代码中沿用原项目的命名、命名空间、模块名、类名前缀或明显相同的业务标识。
- 迁移功能时只能吸收设计思路和实现方式，正式落地必须使用当前项目原创命名。
- 模板或参考资源应保留在参考目录中，不应把参考项目命名扩散到正式业务代码。
- 当前项目的命名空间建议参考 `Docs/总设计文档/GDD_污染区撤离_Codex版.md` 中的 `3.5 命名空间建议`，正式代码优先使用 `CZ` 作为根命名空间，例如：`CZ.Core`、`CZ.Player`、`CZ.Camera`、`CZ.Weapons`、`CZ.Enemies`、`CZ.Inventory`、`CZ.Loot`、`CZ.Survival`、`CZ.UI`。
- 如果后续项目正式名称变化，需要先更新命名空间规则，再批量调整代码命名。

## 四、文档归档规则

- 所有项目文档必须写入 `Docs` 文件夹下，不能散落在项目根目录或资源目录中。
- `Docs` 下固定分为三个文档区域：`Docs/总设计文档`、`Docs/程序文档`、`Docs/策划文档`。
- `Docs/总设计文档` 只存放项目总体设计、顶层模块划分、模块之间的连接关系和整体架构说明；不写单个模块内部的具体玩法细节，也不写具体施工步骤。
- `Docs/策划文档` 只存放单个模块的玩法设计、规则设计、内容设计、数值方向、体验目标和策划方案；不写技术实现、代码结构或开发施工清单。
- `Docs/程序文档` 只存放技术拆解、模块施工清单、开发步骤、接口设计、实现方案、依赖关系和验证方式。
- 新建文档前必须先判断文档类型，再放入对应子目录。

## 五、参考资源使用规则

- `Assets/ProjectRef` 下的内容视为参考资源，不代表最终正式项目架构。
- 可以分析、运行和拆解参考资源，但正式业务代码不得直接继承参考项目的命名体系。
- 如果需要把参考功能迁移到正式项目，应先形成中文技术拆解或施工清单，再按当前项目命名规则重新实现。

## 六、临时美术资源池规则

- `Assets/ArtTemplate` 下的内容视为本地临时美术资源池，用于导入、筛选和对比大量外部美术资源。
- `Assets/ArtTemplate` 不属于正式项目资源目录，不上传 Git。
- 未确认使用的模型、贴图、材质、动画、特效、图标等资源应保留在 `Assets/ArtTemplate`，不能直接混入正式目录。
- 只有确认进入项目的美术资源，才允许整理后放入 `Assets/Game/Art` 下对应子目录。
- 移入 `Assets/Game/Art` 的资源必须按正式目录结构重新归类和命名，不能保留外部资源包的混乱目录结构。

## 七、测试资源与测试代码规则

- `Assets/Game/Scenes` 只允许存放正式游戏场景，不能存放任何测试场景，正式场景命名不能出现 `Test`。
- `Assets/Game/Tests` 只允许存放测试场景和测试场景所需的场景级资源，不允许存放测试代码。
- UI 测试、功能测试、战斗测试、输入测试、摄像机测试、武器测试、AI 测试、性能测试、版本验证测试等测试场景都必须放入 `Assets/Game/Tests` 下对应子目录。
- 临时测试代码、测试脚本、测试辅助工具统一放入 `Assets/TmpTests`。
- `Assets/TmpTests` 不属于正式业务目录，不上传 Git。
- 如果某个测试脚本需要长期保留，必须先经过确认，再决定是否迁移到正式测试体系或正式工具目录。
